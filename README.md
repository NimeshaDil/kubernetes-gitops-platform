# bug-reporter-app

Start all services:

docker-compose up --build


Access:

Frontend: http://localhost:8080

Backend API: http://localhost:3000/feedback

DB: localhost:5432 (use credentials from env)

Stop services:

docker-compose down